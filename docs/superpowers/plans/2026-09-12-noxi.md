# Noxi implementation plan

Approved scope: reusable dialogue, Study Board short explanations, and first-username onboarding. No additional design checkpoints.

1. Add a focused test harness for the shared Noxi section, dialogue evidence boundary, onboarding steps, persistence, and account isolation; confirm it fails before implementation.
2. Add shared character/dialogue rendering in blind-chess.html, using the existing explaining PNG. Add responsive Study Board and accessible onboarding layouts.
3. Connect Study Board to the existing played-move motif analyzer and classification. Use controlled text templates rather than engine prose; never pass suggestions or evaluations to Noxi. Preserve detailed analysis separately.
4. Enroll only at first username save using Supabase user metadata. Persist seen/completed state with per-account local fallback; guard rerenders, logout, and asynchronous account changes. Route completion through lsnEnter().
5. Allowlist the existing image in server/server.py; Docker already copies assets/. Test source behavior, existing review/education/course suites, and HTTP asset delivery. Inspect desktop and narrow browser layouts.

Files: blind-chess.html, server/server.py, server/test_noxi.js, assets/characters/nox-guide-explaining-v1.png (existing), and documentation. No schema migration or deployment required.

## Integration with latest main

The earlier implementation was based on September 7 code. Merge main at `54a20f8`, keeping the current homepage, ten-lesson training course, email authentication and redesigned Study Board. Mount the common Noxi dialogue in `stReasons`, read the one-based selected move from `STUDY.recs`, and retain current detailed analysis. Protect password recovery from onboarding. Re-run Noxi, Study Board, auth, homepage, lesson and asset suites, and verify Noxi on the actual port-8787 server before delivery.
